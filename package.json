{
  "name": "maintainerd-github-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/postbuild.js",
    "preview": "vite preview --host 0.0.0.0",
    "check": "vite build && node scripts/postbuild.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@lucide/svelte": "^1.31.0",
    "marked": "^14.1.3",
    "svelte-routing": "^2.13.0"
  }
}
